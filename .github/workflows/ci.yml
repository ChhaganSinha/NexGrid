name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  javascript:
    name: JS packages (build + typecheck)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run typecheck
      - run: npm test

  dotnet:
    name: TableX.AspNetCore (build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      # The RCL embeds the vanilla bundle as static web assets, so build JS first.
      - run: npm ci
      - run: npm run build:core && npm run build:vanilla
      - run: dotnet build dotnet/TableX.sln -c Release
      - run: dotnet test dotnet/TableX.sln -c Release --no-build
